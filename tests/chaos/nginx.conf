server {
    listen 80;
    listen [::]:80;
    server_name localhost;

    # Return 200 OK with a simple response for all paths
    location / {
        add_header Content-Type text/plain;
        return 200 'OK';
    }
}
