// API routes.

routes {
    route "api" {
        matches {
            path-prefix "/api"
        }
        upstream "api"
        policies {
            timeout-secs 30
            failure-mode "open"
        }
        retry-policy {
            max-attempts 3
        }
    }
}
